export type Skills = {
  input: {
    skills: string;
  };
};

export type UpdateSkills = {
  skillsId: string;
  input: {
    skills: string;
  };
};
export type DeleteSkills = {
  skillsId: string;
};
